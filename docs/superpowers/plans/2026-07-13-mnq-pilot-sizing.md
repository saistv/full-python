# MNQ-First Pilot Sizing Implementation Plan

1. Implement deterministic finite-horizon moving-block path analysis.
2. Lock the reference and adverse-cost scenarios in code and tests.
3. Build a clean-tree, two-trial registered runner using flat 1 MNQ and the
   `$150` strategy/simulation DLL.
4. Report observed metrics, anchored folds, 30-session loss-budget risk, and
   21-session `$5,000` feasibility.
5. Run unit and operator-data suites, commit, execute five-year authority run,
   and record a pass/fail decision without changing signal parameters.

